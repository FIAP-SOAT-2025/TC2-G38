apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: lanchonete-tc2
spec:
  selector:
    app: lanchonete-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30000
  type: NodePort
